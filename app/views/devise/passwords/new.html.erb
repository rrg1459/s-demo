<div class="col-lg-6 col-lg-offset-3">
  <div class="well">
    <h4><b>Olvidó su contraseña?</b></h4>
    <div class="row">
      <div class="col-lg-8 col-lg-offset-1">
        <label>Escriba un correo para recuperar tu cuenta</label><br>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
          <div class="form-group">
            <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "Dirección de Correo" %>
          </div>
          <div class="form-group">
            <%= f.submit "Resetar Contraseña", class: "btn btn-primary" %>
          </div>
          <%= render 'parciales/devise_form_errors' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
