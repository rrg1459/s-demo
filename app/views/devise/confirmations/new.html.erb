<div class="col-lg-6 col-lg-offset-3">
  <div class="well">
    <h4><b>Reenviar confirmacion</b></h4>
    <div class="row">
      <div class="col-lg-8 col-lg-offset-1">
        <label>¿No recibió instrucciones de confirmación? Introduzca su correo electrónico nuevamente y pulse el botón de abajo</label>
        <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
          <div class="form-group">
            <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "Correo" %></div>
          <div class="form-group">
            <%= f.submit "Reenviar confirmacion", class: "btn btn-primary" %>
          </div>
          <%= render 'parciales/devise_form_errors' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
